import React from 'react';
import TitleBar from '../../shared/components/TitleBar';

const TitleDashboard = () => {
  return (
    <TitleBar moduleName="Dashboard">
      <p>Dashboard</p>
    </TitleBar>
  );
};

export default TitleDashboard;