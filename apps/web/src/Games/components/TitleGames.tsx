import React from 'react';
import TitleBar from '../../shared/components/TitleBar';

const TitleGames = () => {
  return (
    <TitleBar moduleName="Games">
      <p>Games</p>
    </TitleBar>
  );
};

export default TitleGames;
